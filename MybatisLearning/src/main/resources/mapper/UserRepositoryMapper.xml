<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
    PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
    "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<!-- 规则: namespace为接口的全类名 -->
<mapper namespace="com.test.repository.UserRepository">

    <!-- 规则: id为接口中对应的方法名 -->
    <insert id="add" parameterType="com.test.entity.User" useGeneratedKeys="true" keyProperty="id">
        insert into user(username, password, age) values(#{username}, #{password}, #{age})
    </insert>

    <delete id="delete" parameterType="java.lang.Integer">
        delete from user where id = #{id}
    </delete>

    <update id="update" parameterType="com.test.entity.User">
        update user set username = #{username}, password = #{password}, age = #{age} where id = #{id}
    </update>
    
    <select id="get" parameterType="java.lang.Integer" resultType="com.test.entity.User">
        select * from user where id = #{id}
    </select>

    <select id="getByNameAge" resultType="com.test.entity.User">
        select * from user where username = #{param1} and age = #{param2}
    </select>

    <select id="getCount" resultType="int">
            select count(*) from user
    </select>

    <select id="getAll" resultType="com.test.entity.User">
        select * from user
    </select>
</mapper>
