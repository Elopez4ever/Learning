<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
    PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
    "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<!--suppress CheckTagEmptyBody -->
<mapper namespace="com.test.repository.StudentRepository">
    
    
    <resultMap id="studentMap" type="com.test.entity.Student">
        <id property="id" column="sid"></id>
        <result property="name" column="cname"></result>
        <association property="clazz" javaType="com.test.entity.Class">
            <id property="id" column="cid"></id>
            <result property="name" column="cname"></result>
        </association>
    </resultMap>

    <select id="getById" resultMap="studentMap" parameterType="java.lang.Integer">
        select s.id sid, s.name sname, cid, c.name cname
        from student s
        join class c on s.cid = c.id
        where s.id = #{id}
    </select>

    <resultMap id="lazyMap" type="com.test.entity.Student">
        <id property="id" column="id"></id>
        <result property="name" column="name"></result>
        <association property="clazz" column="cid" javaType="com.test.entity.Class"
                     select="com.test.repository.ClassRepository.findById"></association>
    </resultMap>

    <select id="findById" resultMap="lazyMap">
        select * from student where id = #{id}
    </select>
</mapper>
